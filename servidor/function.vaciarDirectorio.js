/** * Copyright 2014 - Edgar Alexander Franco * * @author Edgar Alexander Franco * @version 1.7.1 */var fs = require('fs');/** * Elimina el contenido de un directorio. * * @param {string} ruta Ruta del directorio a vaciar. */exports.vaciarDirectorio = function (ruta) {  if (!fs.existsSync(ruta)) {    return;  }    var archivos = fs.readdirSync(ruta);  var total = archivos.length;  var archivo;    for (var i = 0; i < total; i++) {    archivo = archivos[ i ];        if (archivo != '.' && archivo != '..') {      fs.unlinkSync(ruta + archivo);    }  }}